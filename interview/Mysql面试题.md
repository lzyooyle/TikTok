## MyISAM和InnoDB的区别？
1. MyISAM的表由一个文件构成，文件格式是.ibd，由于是存储在文件上的，所以备份恢复比较方便，可以对单个表进行操作，排序的顺序，按照插入的先后顺序进行排序，不支持事务和
2. InnoDB的表由表定义文件，数据文件，引擎文件构成，文件格式是.myd和.myi，备份恢复一般用binlog和mysqldump命令，排序的顺序，按照主键的大小进行排序，支持事务和外键。
